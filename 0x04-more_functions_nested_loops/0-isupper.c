#include "main.h"

/**
 * _isupper - function checks for uppercase
 * @c: character to be check
 * Return: 1 if character is upper, and 0 if not
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
